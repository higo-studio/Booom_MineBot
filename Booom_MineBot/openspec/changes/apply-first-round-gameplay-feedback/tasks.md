## 1. 项目约束与输入资产

- [x] 1.1 将第一轮反馈摘要同步到项目规划文档或 skill 参考，明确自由移动、自动挖墙、标记模式和建筑模式覆盖旧输入假设。
- [x] 1.2 更新 `Assets/InputSystem_Actions.inputactions` 的 Player action map，提供 `Move`、`Scan`、`ToggleMarkerMode`、`ToggleBuildMode`、`PointerPosition`、`PointerClick`、`Cancel`、`Pause` 和升级选择动作。
- [x] 1.3 重新生成并提交 `MinebotInputActions` wrapper，确保代码不再依赖旧的主路径 `Mine`、`ToggleMarker`、`BuildRobot` 动作。
- [x] 1.4 更新 HUD 操作提示和测试辅助输入，使提示与 `WASD`、`Q`、`E`、`R`、鼠标点击的新交互一致。

## 2. 主机器人自由移动与自动挖掘

- [x] 2.1 新增或改造主机器人自由移动组件，使玩家在空地中以连续世界坐标移动，并继续能映射到逻辑格。
- [x] 2.2 实现岩壁、边界和建筑阻挡碰撞，确保主机器人不能穿越不可通行区域。
- [x] 2.3 实现接触格查询组件，将移动方向、碰撞接触点和世界坐标转换为候选逻辑格。
- [x] 2.4 接入持续贴墙自动挖掘状态，按配置节奏向规则服务提交挖掘意图。
- [x] 2.5 验证硬度不足、挖开岩壁、炸药触发、资源/经验奖励和地形刷新仍由现有规则服务结算。

## 3. 模式化交互

- [x] 3.1 在 `GameplayInputController` 中实现 `Normal`、`Marker`、`Build`、`UpgradeLocked`、`GameOver` 等交互模式。
- [x] 3.2 将 `Q` 探测接入新输入路径，并保留能量消耗、数字反馈和能量不足提示。
- [x] 3.3 实现 `E` 标记模式，支持鼠标悬停候选岩壁、点击标记/取消标记、再次按 `E` 或取消输入退出。
- [x] 3.4 确保升级 UI、暂停和失败状态会锁定地图点击、自动挖掘、标记和建造命令。
- [x] 3.5 更新 PlayMode 烟雾测试，覆盖进入标记模式、点击岩壁标记和退出模式。

## 4. 可配置建筑放置

- [x] 4.1 新增建筑定义配置，包含 id、显示名、成本、footprint、允许地形、prefab 和碰撞尺寸。
- [x] 4.2 实现建筑放置服务，校验资源、地形、已有占位、边界和 footprint 内所有格子。
- [x] 4.3 实现 `R` 建筑模式 UI，支持选择建筑、显示合法/非法预览、点击确认和取消退出。
- [x] 4.4 成功建造时扣除资源、创建建筑实例、写入逻辑占位，并实例化 prefab 表现与碰撞。
- [x] 4.5 将默认维修站和机器人工厂迁移或适配到新建筑/设施占位查询，保持维修与生产机器人流程可用。

## 5. 从属机器人自由移动迁移

- [x] 5.1 将从属机器人表现从逐格刷新迁移到连续世界坐标移动或插值。
- [x] 5.2 让从属机器人自动模式目标选择继续基于玩家可见逻辑信息，保持避开标记格、不使用隐藏炸药真相。
- [x] 5.3 接入建筑占位和碰撞阻挡，使机器人遇到不可达目标时进入受阻或重选目标状态。
- [x] 5.4 更新机器人状态 HUD 或反馈，区分自由移动中的工作、待机、受阻和损毁。

## 6. 测试与验证

- [x] 6.1 补充 EditMode 测试：世界坐标到逻辑格映射、接触格选择、建筑 footprint 校验和占位写入。
- [x] 6.2 补充 EditMode 测试：自动挖掘触发时仍遵守硬度门槛、炸药结算和奖励发放。
- [x] 6.3 补充 PlayMode 测试：Gameplay 中 WASD 自由移动、贴墙自动挖掘、`Q` 探测、`E` 标记模式和 `R` 建筑模式。
- [x] 6.4 使用 UnityMCP 执行 `unity.compile`，如 Play Mode 阻塞则按项目 skill 约定退出 Play Mode 后重试。
- [x] 6.5 手动烟雾验证 `Gameplay` 与 `DebugSandbox`：自由移动手感、建筑碰撞、多格占地、从属机器人移动和 HUD 提示均可读。
- [x] 6.6 运行 `openspec validate apply-first-round-gameplay-feedback`，确保 proposal、design、specs 和 tasks 可通过校验。
