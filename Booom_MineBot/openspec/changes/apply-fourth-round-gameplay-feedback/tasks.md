## 1. 规则与配置底座

- [x] 1.1 扩展 `WaveConfig`，加入 50 波危险区厚度表、塌方混雷比例和稳定抽样所需的配置字段。
- [x] 1.2 扩展 `UpgradePoolConfig` / 玩家成长状态，加入显式伤害加值、移动速度、标记数量和两选一随机升级。
- [x] 1.3 新增 `ScoreConfig` / `ScoreService` / 本地排行榜存储入口，并挂入 `BootstrapConfig` / `RuntimeServiceRegistry`。

## 2. 炸弹、危险区与计分

- [x] 2.1 恢复玩家、机器人触发炸弹后的连锁破坏，并补齐奖励、被动感知和表现层刷新。
- [x] 2.2 改造地震塌方回填：按配置比例为新墙体混入炸弹，且使用稳定配额随机。
- [x] 2.3 让地震外围炸弹阶段记录实际炸毁炸弹数量，并结算“炸毁炸弹”“存活过波”计分。
- [x] 2.4 补 EditMode 测试，覆盖连锁破坏、逐波厚度表、50 波后钳制和塌方混雷比例。

## 3. 输入、升级与表现

- [x] 3.1 删除标记模式，改为普通点击墙体切换标记，并接入标记上限。
- [x] 3.2 把升级面板改为每次随机两项，支持最大生命、钻头伤害、移动速度和标记数量升级。
- [x] 3.3 让移动速度升级实时影响自由移动速度，并让地震炸弹也播放爆炸特效。
- [x] 3.4 补 PlayMode / EditMode 测试，覆盖直接点击标记、两选一升级和地震爆炸表现触发。

## 4. 启动页与排行榜

- [x] 4.1 为 `Bootstrap` 场景增加启动页，提供开始游戏、退出游戏和本地排行榜摘要。
- [x] 4.2 在失败界面显示本局分数并允许输入名字保存到本地前十排行榜。
- [x] 4.3 补测试或最小烟雾验证，确认启动页、失败录分和 `PlayerPrefs` 读写流程可用。
- [x] 4.4 把启动页与失败录分界面迁移到项目现有 UI prefab 体系，移除 `OnGUI` 即时绘制。

## 5. 验证与收口

- [x] 5.1 使用 UnityMCP 运行 `unity.compile`，并执行本次相关 EditMode / PlayMode 测试。
- [x] 5.2 运行 `openspec validate apply-fourth-round-gameplay-feedback`，确认 proposal / design / specs / tasks 结构完整。
