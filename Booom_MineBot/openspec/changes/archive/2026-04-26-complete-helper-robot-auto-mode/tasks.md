## 1. 自动模式数据与配置

- [x] 1.1 扩展 `RobotState`，增加自动模式状态、当前目标、状态原因和行动计时数据。
- [x] 1.2 定义机器人自动模式结果类型，用于表达移动、挖掘、待机、受阻、损毁和奖励结算。
- [x] 1.3 将机器人搜索半径、行动间隔、可挖硬度策略和损毁回收资源接入现有平衡配置或新增轻量配置。
- [x] 1.4 保持现有机器人工厂生产流程兼容，确认新字段有稳定默认值。

## 2. 目标选择与寻路

- [x] 2.1 重写目标资格判断，移除目标选择阶段对隐藏 `HasBomb` 的避险读取。
- [x] 2.2 实现合格目标规则：可挖、未标记、非危险区、硬度可处理、搜索范围内、存在可达相邻落脚格。
- [x] 2.3 实现基于 BFS 的可通行路径查找，选择目标相邻落脚格并返回下一步移动方向。
- [x] 2.4 实现目标失效处理：目标被挖掉、被标记、进入危险区或路径阻断时清除并重选。
- [x] 2.5 为目标选择、标记避让、危险区避让、不可达目标和确定性 tie-break 补充 EditMode 测试。

## 3. 自动挖掘与风险结算

- [x] 3.1 从 `MiningService` 抽出共享挖掘入口，使玩家和机器人都能复用硬度、地形转换和奖励逻辑。
- [x] 3.2 实现单机器人 tick：待机选目标、沿路径移动、相邻后挖掘、受阻时记录状态。
- [x] 3.3 实现多机器人 tick 入口，并在升级选择待处理或玩家死亡时暂停自动模式。
- [x] 3.4 结算机器人成功挖掘奖励，将金属、能量和经验同步到当前单局资源与成长系统。
- [x] 3.5 结算机器人误挖未标记炸药：机器人损毁、爆炸地形结算、不对玩家应用直接挖雷伤害。
- [x] 3.6 为移动挖掘、奖励入账、误挖炸药损毁、升级暂停和无目标待机补充 EditMode 测试。

## 4. 表现层与反馈

- [x] 4.1 在 `MinebotGameplayPresentation.Update` 中调用运行时机器人 tick 入口，并保持表现层不直接修改规则状态。
- [x] 4.2 刷新从属机器人 Sprite 位置、显隐和状态表现，确保移动、待机、受阻、损毁都有可读反馈。
- [x] 4.3 更新 HUD 或反馈文本，显示活跃机器人数量、工作/待机/受阻状态和最近机器人收益或损毁事件。
- [x] 4.4 确认 `Gameplay` 与 `DebugSandbox` 复用同一自动模式表现路径。

## 5. 验证与回归

- [x] 5.1 更新 PlayMode 测试，验证生产机器人后能自动移动或挖掘，并刷新地图和机器人表现。
- [x] 5.2 验证机器人不会自动使用探测，不会挖玩家标记格，不会因为隐藏炸药真相而提前避开未标记炸药。
- [x] 5.3 验证地震波结算仍能摧毁危险区机器人并回收资源，且自动模式不会破坏现有波次失败规则。
- [x] 5.4 使用 UnityMCP 运行 `unity.compile`、EditMode 测试和 PlayMode 测试。
- [x] 5.5 手动烟雾验证 `Gameplay`：造机器人后观察自动目标选择、移动、挖掘、待机/受阻提示和危险区损毁反馈。
- [x] 5.6 运行 `openspec validate complete-helper-robot-auto-mode`，确认提案和规格有效。
