## 1. 文档与规则边界

- [x] 1.1 新增 `replace-active-scan-with-passive-hazard-sense` change，并明确主动探测删除、周期性被动感知接管。
- [x] 1.2 在 spec 中明确：被动感知不再消耗能量，且不再依赖 `Q` 或 HUD 主动按钮触发。
- [x] 1.3 在 spec / design 中明确：安全墙按 `3x3` 炸药数触发连锁破碎，炸弹改为单格惩罚。

## 2. 运行时实现

- [x] 2.1 为 `HazardRules` / `GameSessionService` 增加被动感知时间间隔与自动刷新路径，移除主动 `Scan()` 主路径。
- [x] 2.2 保留前沿岩壁筛选与 `3x3` 炸药计数规则，但改为周期性基于玩家当前位置自动刷新。
- [x] 2.3 将单格挖掘改成“安全墙连锁破碎 / 炸弹单格惩罚”，并让玩家与机器人共用同一结算口径。
- [x] 2.4 更新 `MinebotGameplayPresentation` / `GameplayInputController` / HUD 按钮文案，使扫描数字改为被动更新且不再刷普通反馈消息。

## 3. 回归验证

- [x] 3.1 更新 EditMode 测试，覆盖“被动感知不耗能”“按间隔自动刷新”“前沿岩壁数字仍正确”。
- [x] 3.2 更新 EditMode / PlayMode 测试，覆盖“零风险安全墙会连锁破碎”“带数字安全墙只破碎当前格”“炸弹只炸开当前格并扣血”。
- [x] 3.3 更新 PlayMode 测试，覆盖“没有主动探测输入也能看到周边扫描数字”“HUD/按钮文案不再要求 `Q` 探测”。
- [ ] 3.4 运行 OpenSpec 校验，并在可用时补做 Unity 编译 / 测试验证。
